

/*
 * every time you make a new change: save, cmake --build .
 * glm::perspective(fovy, aspect, near, far)
 * gluLookAt() shows where your camera is looking
 * you can translate, rotate, and scale
 *
 * There is shader code and C++ code
 *  create uniform variables in C++ to use in shader code
 */